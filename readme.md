Static html page designed to be served via Github Pages/Jekyll.

Test locally with `bundle exec jekyll serve`.

Pre-reqs (run from project root)-
1. Install ruby & gem
2. `gem install bundler`
3. `bundle init`
4. `bundle add github-pages webrick`
